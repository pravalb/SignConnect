import { WritingQInitialState } from "./definitions";
export const WritingQReducer = (state, action) => {
  switch (action.type) {
    case 'handleResponse':
      return { ...state,
        inputValue: action.response
      };

    case 'correctAnswer':
      return { ...state,
        inputValue: action.response,
        inputEnabled: false,
        grade: 'correct',
        continueEnabled: true
      };

    case 'giveUp':
      return { ...state,
        inputEnabled: false,
        grade: 'incorrect',
        continueEnabled: true,
        inputValue: action.payload.answer
      };

    case 'reset':
      return { ...WritingQInitialState
      };

    default:
      return state;
  }
};
//# sourceMappingURL=reducer.js.map