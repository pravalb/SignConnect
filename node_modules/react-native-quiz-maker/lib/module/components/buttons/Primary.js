import * as React from 'react';
import { Dimensions, SafeAreaView, StyleSheet, Text, TouchableHighlight } from 'react-native';

const PrimaryButton = props => /*#__PURE__*/React.createElement(SafeAreaView, {
  style: [styles.buttonContainer, props.buttonContainerStyle]
}, /*#__PURE__*/React.createElement(TouchableHighlight, {
  onPress: () => props.onPress(),
  disabled: !props.enabled,
  style: [styles.button, props.buttonStyle]
}, /*#__PURE__*/React.createElement(Text, {
  style: [styles.label, props.labelStyle]
}, props.label)));

const styles = StyleSheet.create({
  buttonContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },
  button: {
    backgroundColor: 'blue',
    width: Dimensions.get('screen').width / 3,
    height: Dimensions.get('screen').width / 10,
    justifyContent: 'center',
    borderRadius: 10
  },
  label: {
    color: 'white',
    textAlign: 'center'
  }
});
export default PrimaryButton;
//# sourceMappingURL=Primary.js.map