import type { MatchingCard, MatchingQState, QuestionAnswerPair } from './definitions';
declare type State = MatchingQState;
declare type Actions = {
    type: 'selectCard';
    payload: {
        selection: MatchingCard;
        submit: (isCorrect: boolean) => void;
    };
} | {
    type: 'reset';
    payload: {
        questionAnswerPairs: Array<QuestionAnswerPair>;
    };
};
export declare const setup: (x: any) => any;
export declare const matchingReducer: (state: State, action: Actions) => {
    deck: any;
    continueEnabled: boolean;
};
export {};
