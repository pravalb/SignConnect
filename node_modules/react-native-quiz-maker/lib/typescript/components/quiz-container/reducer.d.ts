import type { QuestionProps, QuizContainerState } from './definitions';
export declare const isLastElement: (index: number, arr: any[]) => boolean;
declare type Actions = {
    type: 'nextSlide';
} | {
    type: 'exit';
} | {
    type: 'updateProgress';
} | {
    type: 'userSubmit';
    payload: {
        isCorrect: boolean;
        progressIncrement: number;
    };
} | {
    type: 'setQuestionComponents';
    payload: {
        questionComponents: QuestionProps;
    };
};
export declare const QuizContainerReducer: (state: QuizContainerState, action: Actions) => QuizContainerState;
export {};
