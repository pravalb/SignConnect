import { Animated } from 'react-native';
declare const springAnimation: (elem: Animated.Value | Animated.ValueXY, toValue: number | any) => Animated.CompositeAnimation;
export default springAnimation;
