import { Animated } from 'react-native';
declare const timedAnimation: (elem: Animated.Value | Animated.ValueXY, time: number, toValue: number | any) => Animated.CompositeAnimation;
export default timedAnimation;
