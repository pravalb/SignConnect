"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.sw = exports.normalize = void 0;

var _reactNative = require("react-native");

// based on iphone 5s's scale
const scale = _reactNative.Dimensions.get('screen').width / 320;

const normalize = size => {
  const newSize = size * scale;

  if (_reactNative.Platform.OS === 'ios') {
    return Math.round(_reactNative.PixelRatio.roundToNearestPixel(newSize));
  } else {
    return Math.round(_reactNative.PixelRatio.roundToNearestPixel(newSize)) - 2;
  }
};

exports.normalize = normalize;

const sw = _reactNative.Dimensions.get('screen').width;

exports.sw = sw;
//# sourceMappingURL=sizing.js.map