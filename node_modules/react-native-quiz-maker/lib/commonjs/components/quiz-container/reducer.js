"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.QuizContainerReducer = exports.isLastElement = void 0;

const isLastElement = (index, arr) => index === arr.length - 1;

exports.isLastElement = isLastElement;

const QuizContainerReducer = (state, action) => {
  if (action.type === 'nextSlide') {
    return { ...state,
      activeQuestion: state.activeQuestion + 1,
      continueEnabled: false
    };
  } else if (action.type === 'userSubmit') {
    return { ...state,
      continueEnabled: true,
      progress: action.payload.isCorrect ? state.progress + action.payload.progressIncrement : state.progress
    };
  } else {
    return state;
  }
};

exports.QuizContainerReducer = QuizContainerReducer;
//# sourceMappingURL=reducer.js.map